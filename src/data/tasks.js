// Mock data for tasks - Now fetched from Firestore via useTasks hook for TasksPage
// This file is kept to prevent import errors in other components (e.g. Dashboard)
// that might still be using it directly. They should be updated to use the hook.
export const mockTasks = [];

export default mockTasks;
